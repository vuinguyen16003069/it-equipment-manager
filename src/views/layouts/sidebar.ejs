<nav class="w-60 bg-gradient-to-b from-gray-900 to-gray-800 text-white flex flex-col shrink-0 shadow-lg">
    <div class="px-6 py-6 border-b border-gray-700">
        <h2 class="text-lg font-bold tracking-wide flex items-center gap-2">💻 IT Manager</h2>
        <p class="text-xs text-gray-400 mt-1">v1.0.0</p>
    </div>

    <ul class="flex-1 px-3 py-4 space-y-1 overflow-y-auto">
        <li>
            <a href="/equipment"
               class="flex items-center gap-3 px-4 py-3 rounded-lg hover:bg-gray-700 text-sm font-medium transition-all cursor-pointer active:scale-95 <%= request.path === '/equipment' ? 'bg-blue-600 text-white shadow-lg' : 'text-gray-200' %>">
                &#128421; Danh sách thiết bị
            </a>
        </li>
        <% if (locals.user && locals.user.role === 'admin') { %>
        <li>
            <a href="/equipment/create"
               class="flex items-center gap-3 px-4 py-3 rounded-lg hover:bg-gray-700 text-sm font-medium transition-all cursor-pointer active:scale-95 <%= request.path === '/equipment/create' ? 'bg-blue-600 text-white shadow-lg' : 'text-gray-200' %>">
                &#10133; Thêm thiết bị
            </a>
        </li>
        <li class="pt-3 mt-3 border-t border-gray-700">
            <a href="/users"
               class="flex items-center gap-3 px-4 py-3 rounded-lg hover:bg-gray-700 text-sm font-medium transition-all cursor-pointer active:scale-95 <%= request.path.startsWith('/users') ? 'bg-blue-600 text-white shadow-lg' : 'text-gray-200' %>">
                &#128100; Quản lý người dùng
            </a>
        </li>
        <% } %>
    </ul>

    <div class="px-6 py-4 border-t border-gray-700 text-xs text-gray-500">
        <p>&copy; 2026 vuinguyen16003069</p>
    </div>
</nav>
